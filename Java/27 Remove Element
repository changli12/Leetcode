class Solution {
    public int removeElement(int[] nums, int val) {
        int result = 0;
		int index = 0;
		while(index < nums.length)
		{
			if(nums[index] != val)
				nums[result++] = nums[index];
			index++;
		}
        return result;

    }
}